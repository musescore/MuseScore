init init/Treble.mscx
cmd note-input
cmd note-a
cmd escape
# Crashed at the following line.
# A potential crash happens only if ScoreView gets created
# (or something else uses CmdState's element field).
palette Glissando subtype 0
cmd undo
cmd undo
cmd escape
test score init/Treble.mscx
